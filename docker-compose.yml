services:
  api:
    build: .
    env_file: .env
    ports: ["127.0.0.1:3000:3000"] # bound to localhost; Nginx fronts it
    depends_on: [redis]
    restart: always
  redis:
    image: redis:7-alpine
    restart: always
    volumes: [redis-data:/data]
volumes:
  redis-data:
